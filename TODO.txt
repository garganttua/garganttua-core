0/ Créer les ContextualSupplier
1/ [DONE] implement auto detection for @Resolvers
   - Auto-detection implemented in InjectionContextBuilder.doAutoDetection()
   - Scans packages for @Resolver annotated classes
   - Automatically registers resolvers for their target annotations
2/ [DONE] implement auto detection for @ChildContext => register a new child context factory
   - Auto-detection implemented in InjectionContextBuilder.doAutoDetection()
   - Scans packages for @ChildContext annotated classes
   - Automatically registers child context factories
3/ [DONE] Maybe remove the function context(IDiContextBuilder context) in IRuntimesBuilder => this method is used to register the runtime resolvers and child context factory. If the todos 1/ an 2/ are done, then this method is obsoleth an can be deleted in favor of auto-detection
   - Manual registration removed from RuntimesBuilder.setupInjectionContext()
   - All runtime resolvers now use @Resolver annotation
   - RuntimeContextFactory now uses @ChildContext annotation
   - Legacy code kept as comments for reference
4/ Create a @ScanPackage() auto detected by builders that implements IPackageableBuilder in order to auto-detect packages to scan for reflection, faire une classe PackageAutoDetect dans DSL
6/ Implements distributed locks and mutex system over redis in a binding module
7/ Add built-in conditions as expression nodes
8/ Implémenter un nativeConfigurationbuilder pour garganttua-expression
10/ Implémenter un nativeConfigurationbuilder pour garganttua-runtime