/**
 * Expression functions for runtime workflow operations, exposing runtime capabilities
 * to the expression and script engines.
 *
 * <p>The functions are {@code @Expression}-annotated static methods (see
 * {@link com.garganttua.core.runtime.functions.RuntimeFunctions}) that bridge the
 * current {@link com.garganttua.core.runtime.IRuntimeContext} into expression and
 * script evaluation.</p>
 *
 * @since 2.0.0-ALPHA01
 */
package com.garganttua.core.runtime.functions;
